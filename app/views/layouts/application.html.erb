<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
  "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html>
<head>
  <title>Recipieces - ya ain't livin if ya ain't eatin good!</title>
  <meta name="Author" content="Reese J Hodge IV" />
  <meta name="Keywords" content="free recipes,health food,healthy recipes" />
  <meta name="Description" content="Recipieces goal is to become the world's largest and most authoritative resource for recipes of all kinds. Free recipes, private recipes, buy trade sell you name it." />
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults, :cache => true %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div id="user_nav">
    <% if user_signed_in? %>
      Signed in as <%= current_user.name %>. Not you?
      <%= link_to "Sign out", destroy_user_session_path %>
    <% else %>
      <%= link_to "Sign up", new_user_registration_path %> or
      <%= link_to "Sign in", new_user_session_path %>
    <% end %>
  </div>
  <div id="mainContent">
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </div>
</body>
</html>
